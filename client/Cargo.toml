[package]
name = "client"
version = "0.1.0"
edition = "2024"
[dependencies]
bevy = "0.19.1"
bevy_replicon = { version = "0.43.0", default-features = false, features = ["client"] }
bevy_replicon_renet = { version = "0.19.0", default-features = false, features = ["client", "renet_netcode"] }
rand = "0.10.2"
shared = { version = "0.1.0", path = "../shared" }
